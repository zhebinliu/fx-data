export const PAGE_KNOWLEDGE: Record<string, string> = {
    '/import': `
**当前页面：数据导入 (Data Import)**

**功能简介**
此页面用于将 Excel 或 CSV 文件中的数据批量导入到 FxCRM 的指定对象中（如线索、客户、联系人等）。

**操作步骤**
1. **上传文件**：点击上传区域或拖拽 Excel/CSV 文件。
2. **选择对象**：选择要导入的目标 CRM 对象（如 Leads, Accounts）。
3. **映射字段**：
   - 左侧列出 Excel 表头，右侧选择对应的 CRM 字段。
   - 必填字段必须映射。
   - **特殊字段解析**：
     - **人员字段**：如果 Excel 中是姓名（如"张三"），系统会自动解析为 CRM 用户 ID。
     - **日期字段**：支持多种日期格式自动转换。
4. **导入选项**：
   - **遇到重复**：可以选择"跳过"或"覆盖"。
   - **唯一键**：选择用于判重的字段（如手机号、邮箱）。
5. **开始导入**：点击"开始导入"按钮，系统会显示进度条和成功/失败计数。

**常见问题**
- **Q: 为什么人员解析失败？**
  A: 请确保 Excel 中的姓名与 CRM 中的用户姓名完全一致。建议优先使用手机号进行匹配。
- **Q: 导入速度慢？**
  A: 如果数据量超过 1 万条，建议分批导入。
`,

    '/data-processing': `
**当前页面：数据处理 (Data Processing)**

**功能简介**
提供高级数据清洗和转换工具，支持 VLookup 和批量数据操作。

**核心功能**
1. **连接管理 (Connection Manager)**：
   - 配置外部数据源（如另一个 CRM 实例或数据库）。
   - 用于跨系统数据比对。
2. **VLookup (跨表匹配)**：
   - 类似于 Excel 的 VLookup。
   - **用法**：选择左表（当前数据）和右表（外部数据），指定连接键（如手机号），将右表的某列值填充到左表。
3. **批量操作**：
   - 支持批量查找替换、正则提取、格式化（如手机号格式化）。

**操作建议**
- 在执行大规模 VLookup 前，建议先用少量数据测试匹配准确性。
`,

    '/data-query': `
**当前页面：数据查询 (Data Query)**

**功能简介**
使用 FQL (FxCRM Query Language) 或可视化构建器查询 CRM 数据。

**操作指南**
1. **选择对象**：在左上角下拉框选择要查询的对象（如线索、商机）。
2. **构建筛选器**：
   - 点击"添加筛选条件"。
   - 选择字段（如"创建时间"）、操作符（如"大于"）和值。
3. **FQL 高级查询**：
   - 点击"切换到 FQL 模式"。
   - 输入类似 SQL 的查询语句，例如：\`name like '张%' AND status = 'New'\`。
4. **导出数据**：查询结果可以导出为 Excel。

**Tips**
- 默认只显示前 100 条数据，滚动到底部可加载更多。
- 点击表头可以进行排序。
`,

    '/data-update': `
**当前页面：数据更新 (Data Update)**

**功能简介**
用于批量更新已存在的 CRM 记录。

**操作模式**
1. **基于 ID 更新**：Excel 中必须包含记录的唯一 \`_id\` 字段。系统会根据 ID 精确更新字段。
2. **基于唯一键更新**：如果不知道 ID，可以指定"唯一键"（如手机号）。系统会先查找匹配的记录，再进行更新。

**注意事项**
- **谨慎操作**：批量更新不可逆，建议更新前导出备份。
- 空值处理：如果 Excel 中某单元格为空，可以选择"忽略"或"清空 CRM 字段"。
`,

    '/workflows': `
**当前页面：工作流自动化 (Workflows)**

**功能简介**
构建自动化的 ETL (Extract-Transform-Load) 数据管道。

**组成部分**
1. **触发器 (Source)**：
   - 定义数据来源（如定时从 FTP 获取 Excel，或从 API 拉取）。
2. **转换 (Transform)**：
   - 定义数据清洗规则（如 VLookup, 格式转换, 字段映射）。
3. **执行 (Destination)**：
   - 定义数据去向（如写入 CRM 对象）。

**操作步骤**
- 点击"新建工作流"。
- 拖拽步骤组件到画布。
- 连接步骤，配置每个节点的参数。
- 点击"保存"并"运行"。
`,

    '/admin/users': `
**当前页面：用户管理 (User Management)**

**功能简介**
管理员用于管理系统用户、角色和权限。

**功能列表**
1. **新建用户**：创建新账号，分配角色（Admin/User）。
2. **权限分配**：
   - **Import**: 允许导入数据。
   - **Update**: 允许更新数据。
   - **Query**: 允许查询数据。
   - **Workflow**: 允许创建工作流。
3. **重置密码**：为忘记密码的用户重置密码。
4. **删除用户**：移除离职人员账号。
`
};

export const DEFAULT_KNOWLEDGE = `
**FxCRM 数据工具助手**

我是您的智能助手，可以协助您完成数据导入、清洗、更新和自动化任务。
您可以问我：
- "怎么导入线索数据？"
- "如何配置 VLookup？"
- "工作流怎么用？"

请根据您所在的页面，参考具体的功能指南。
`;
